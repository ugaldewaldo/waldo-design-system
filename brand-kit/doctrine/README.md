# Doctrine

One file per content type: when to use the pattern, what varies, what's fixed, and copy guidance. This is what the generation skill reads first to pick the right pattern for a prompt.

Format: `<pattern>.md` (e.g. `partnership.md`, `welcome-email.md`). Written as patterns ship.
